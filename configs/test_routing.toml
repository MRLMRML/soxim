# Test configuration for routing algorithm comparison

[cycles]
total = 10000
warmup = 3000
measurement = 3000

[microarchitecture]
buffer_size = 8
virtual_channel_number = 8

[routing]
algorithm = "MAD"  # Test MAD algorithm

[topology]
dimension = [ 4, 4, 1 ]
shape = "MESH"

[traffic]
alpha = 0.5
beta = 0.5
flit_size = 1
injection_process = "periodic"
injection_rate = 0.05
packet_size = 20
packet_size_option = "fixed"
traffic_pattern = "random uniform"
